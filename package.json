{
  "private": true,
  "scripts": {
    "size": "yarn size-limit"
  },
  "devDependencies": {
    "@size-limit/preset-app": "^2.2.2"
  },
  "resolutions": {
    "size-limit": "https://github.com/pustovalov/size-limit#build"
  }
}
